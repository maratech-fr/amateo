# Architecture Decision Records — Index

This index is the entry point for the repo's ADRs; add one numbered file per structural decision as it is made (or back-filled), and link it from the table below.

## Convention
- File: `docs/architecture/adr-NNNN-short-title.md` (zero-padded, incrementing).
- Status: `proposed` · `accepted` · `superseded by adr-XXXX` · `deprecated`.
- An ADR is warranted when a decision is **structural** (boundaries, data model, cross-zone contract, security model, infra topology) — not for routine changes.

## Template
```
# ADR-NNNN — <title>
- Status: <proposed|accepted|…>   Date: <YYYY-MM-DD>
- Context: <forces, constraints, what made this a decision>
- Decision: <what was chosen>
- Consequences: <trade-offs, follow-ups, what this rules out>
- Alternatives considered: <options + why rejected>
```

## Index
| ADR | Title | Status |
|-----|-------|--------|
| [ADR-0001](adr-0001-single-pass-solve.md) | Single-pass solve, no silent fallback | accepted — amended 2026-07-07 (two-PHASE lexicographic objective), 2026-07-10 (A10 complexity cap) and 2026-08-17 (third lexicographic tier: generation stability, P3-21) |
| [ADR-0002](adr-0002-pattern-plan.md) | Pattern « Plan » : un plan nommé, des versions, un pointeur | accepted — amended 2026-07-17 (les contraintes datées du FAIT restent sur `CalendarEntry`), 2026-07-24 (**#8**: inv. 5 — une période POSSÈDE sa grille, copie et non union ; inv. 14 — reprendre le socle détruit tout plan de période **pas encore commencée**, validé ou non) 2026-08-18 (inv. 5 étendue à `ImplicitRuleSetting` — une période possède SES 4 règles de bien-être, même patron copie ; lot bien-être-par-période, PR1 backend + PR2 front, P2-35) and 2026-08-18 (**P2-38**: inv. 4 — « deux plans ne se chevauchent jamais » ; PR1 rend une fermeture datée transversale à tout plan qu'elle recoupe, PR2 tient enfin l'invariant PAR LE CODE à la naissance d'un plan de période, refus 409 `window_already_planned` dans les deux sens ; PR3 front (le refus s'affiche à l'endroit du geste) ferme le lot — les 3 PR livrées 2026-08-18) and 2026-08-18 evening (**indispo gymnase informative**: `VenuePeriodOverride.mode` devient nullable, gagne un masque `dayOverrides` composé avec l'incident dans `PlanVenueClosures::effectiveStateForPlan/Entry` — supplante le refus 422 « réactiver un gymnase entièrement fermé » posé le même jour au matin par P2-37 PR1 ; PR2 front livrée depuis — P2-37 lot SOLDÉ, a quitté la roadmap) and 2026-08-18 night (**P2-41 PR-B backend**: le SEGMENT — bloc de semaines calendaires pleines et contiguës — devient l'unité hors socle, la semaine simple restant le segment de taille 1 ; `assertValidWeekChild` remplace le plafond ≤7 j par une borne d'enveloppe (incluse dans les semaines couvrant la mère) + des bornes pleines lun→dim, clamp saison admis ; anti-chevauchement et 409 `window_already_planned` valent tels quels) and 2026-08-19 (**P2-41 PR-C frontend, le lot se solde**: `WeekPickerDialog` propose désormais les segments aux ruptures géométriques, précochés, scindables et fusionnables — l'item a quitté la roadmap) and 2026-09-04 (**D3 v1 SOLDÉE ENTIÈRE — PR-1 backend puis PR-2 cockpit, ex-P2-57**: le gel de FENÊTRE (invariant C1) se lève pour UNE racine CLOSURE sans semaines-enfants — `PUT` re-date dans les deux sens, la fenêtre du plan/les contraintes `venue_closed` nées du même geste/le titre suivent, une fenêtre déjà prise reste refusée en 409 ; tout le reste de l'identité — kind/type/vacances — et toutes les autres racines/semaines-enfants restent gelées ; le geste d'édition à l'écran vit dans la liste du jour du cockpit, `DayDialog.tsx`, bouton « Modifier les dates » ssi `entry.redatable`) and 2026-09-05 (**découpage début·milieu·fin, décision fondateur** : le découpage d'une FERMETURE en semaines-enfants n'est plus libre — P2-41 (scinder/fusionner à la main) reste vrai pour les VACANCES seulement ; une closure se découpe désormais en au plus trois segments imposés — début (semaine entamée), milieu (semaines pleines contiguës, un trou de vacances ou une fenêtre déjà planifiée le coupe en deux runs), fin (semaine entamée) — gardés côté serveur par `App\Service\WeekSegmentationRule`/`ClosureSegmentation`, miroir déclaré front `cockpit/lib/weekSegmentation.ts`, parité `WeekSegmentationMirrorParityTest` ; « Adapter d'un bloc » et le re-datage D3 refusent désormais une fenêtre à plus d'un segment ; une mère déjà découpée en enfants n'est pas re-datable — **D3 v2** ouverte en roadmap) |
| [ADR-0003](adr-0003-match-placement-solve.md) | Le solve de placement des matchs : second problème engine (`/place-matches`), rail synchrone, best-effort à poids dominant, ancres `placementSource` | accepted (2026-08-03, P1-4 PR D) |
| [ADR-0004](adr-0004-period-plan-birth-as-socle-copy.md) | L'adaptation naît comme une COPIE du socle : la V1 d'un plan de période transcrit (sans solveur) la version pointée du socle, filtrée de la sélection de période ; verrous HARD révocables, pas de pointage auto, réduction déterministe | accepted (2026-08-19, P2-44 PR-1) — amended 2026-08-20 (**PR-3, le comblement**: `POST /api/schedules/{id}/fill`, solve PARTIEL borné aux séances « à replacer », placements de la source épinglés HARD dans le payload seul, zéro changement moteur) and 2026-08-20 (**PR-4, remplace sa description initiale**: la transcription devient le DÉFAUT — auto-déclenchée, frontend seul, à l'arrivée sur l'étape Génération d'un plan de FERMETURE vierge ; les VACANCES en restent exclues, à l'octet près, pour une raison de sens ET une raison technique — `OrphanPinGuard` refuserait Régénérer et Combler sur une grille de reprise réécrite) and 2026-09-02 (**PR-3 du lot « overlay au moindre effort », P2-58**: le comblement gagne une RÉFÉRENCE socle — le solve partiel de `/fill` émet désormais au moteur `socleReferenceAssignments` [placements `{teamId, dayOfWeek, startTime}` de la version pointée du socle, sans `venueId`], que le moteur récompense d'un bonus de placement par tier ; contrat backend⇄engine 2.19→2.20, `SCORE_FORMULA_VERSION` V12→V13 — détail : `backend/docs/backend-inventory.md` §« Référence socle du comblement », `engine/docs/constraint-vocabulary.md`) |

*Non-ADR hosted here*: [`constraint-matrix.md`](constraint-matrix.md) — the UI ↔ engine constraint matrix (structuring axis §7.1 "constraint semantics"), including why a HARD lock overrides an entered HARD constraint and how it now says so.

## Candidate decisions to formalize
These are existing, load-bearing decisions found during onboarding that are currently implicit. Promote to ADRs when touched (do not invent rationale retroactively without confirming intent):

1. **Multi-tenant isolation = Doctrine `TenantFilter` + `ClubUser` membership check + PostgreSQL RLS** (GUC `app.club_id` set through `set_config`, session-scoped — **never `SET LOCAL`**, which is a no-op outside a transaction). Security-critical, guarded by `TenantIsolationTest` and `RlsIsolationTest`. Refs: `backend/docs/TENANT.md`, `backend/docs/RLS.md`, `../security/rls.md`.
2. **Backend↔engine contract is hand-synced (no codegen)**, versioned via `engine/CONTRACT_VERSION`, guarded by `ContractSchemaTest`. Why no codegen?
3. **Async generation via Symfony Messenger + Redis + per-club lock**, progress over Mercure (`club:{id}:schedule:{id}`). Why this topology over synchronous generation.
*Formalized / resolved:* the two-pass fallback decision → [ADR-0001](adr-0001-single-pass-solve.md); the **solver budget** (adaptive 60/180/600 s tiers, the payload's `solver_timeout_seconds` acting as a ceiling only, plus the A10 complexity cap) → ADR-0001, amendments 2026-07-07 and 2026-07-10; the Rector 8.3-vs-8.4 mismatch was fixed in code (Rector now targets 8.4), not via ADR — see roadmap §Dette (B1, résolu).
