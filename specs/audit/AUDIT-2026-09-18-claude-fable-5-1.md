# Audit ClubScheduler (Amateo) — édition 2026-09-18

| Méta | Valeur |
|---|---|
| Date | 2026-09-18 |
| Modèle | `claude-fable-5-1` (Fable 5.1, Anthropic) |
| HEAD | `870194bf` (`main`, PR #918 en attente de merge : `restart: unless-stopped` sur le worker de dev — hors HEAD) |
| Méthode | 7 agents d'analyse parallèles (doc, backend, engine, frontend avec exécution, UX, alignement 3 couches, cyber A1–A23) + checks directs (supply chain ×3 EXÉCUTÉS, Mercure, secrets, prod-readiness, RGPD, réversibilité des migrations) + **Behat complet EXÉCUTÉ** (60 scénarios, 316 étapes, génération de saison COMPLETED, 433 s de mur) + **parcours navigateur EXÉCUTÉ sur la base réelle** (compte gestionnaire, lecture seule : axe WCAG 2.x AA sur 11 écrans × 2 thèmes, 2 relevés clavier, piège de focus d'une modale) + vérification contradictoire manuelle de tous les findings ≥ Moyenne (ALIGN-11, BCK-24, BCK-23, ENG-41, ENG-42, SEC-19, SEC-21, A11Y-22, UXS-08, DOC-45) |
| Édition précédente | `AUDIT-2026-08-27-claude-fable-5.md` (HEAD `f4cc22fa`) — depuis : **243 commits, 912 fichiers** (module matchs : conflits coach↔joueur avec rôle par côté, résolution des conflits P4-207, Configuration & navigation, Calendrier unique (fusion Semaine + Consulter, rail supprimé), extérieurs dans la grille, auto-localisation des adversaires, écart de salle arbitré, détail par côté des conflits, case « À confirmer », D1 étendu échauffement même gymnase, ville de l'adversaire, nouveaux défauts + filtres Traitement/domicile, mémoire de session ; registre de parité HARD engine ; découpe `CustomRoutesOpenApiFactory` ; contrat 2.21 ; Mercure épinglé v0.24.2 ; lot Dependabot API Platform 4.4) |

---

## Tableau de couverture

| Axe | Couverture | Détail |
|---|---|---|
| Documentation | ✅ couvert | 11 sondages contre le code (CLAUDE.md 6/6 EXACT, project-map 2 FAUX + 1 imprécis, 3 docs d'alignement 6/6 EXACT au numéro de ligne mais **poids +60/−60 faux dans 2 sur 3**) ; DOC-34/35/36 re-vérifiés ; 0 stamp menteur sur 38 |
| Besoin produit | ✅ couvert | file de la passe de tests du 16-17/09 entièrement livrée (#897→#917), 10/11 livraisons tracées en état des lieux |
| Code backend | ✅ couvert | statique ; 12 migrations relues une à une (RLS 100 %) ; tables globales suivies écrivain par écrivain ; purgers croisés contre les 45 entités tenant |
| Code engine | ✅ couvert | statique ; contrat 2.21 aux 4 sites + garde ; 13/13 clés parsées consommées ; registre de parité HARD lu en entier ; `/place-matches` champ par champ |
| Code frontend | ✅ couvert | `tsc -b --force` + eslint EXÉCUTÉS (0/0) ; **vitest EXÉCUTÉ : 3024 tests / 293 fichiers, 100 % verts au run 1, 0 retry**, image tooling rebâtie avant |
| Supply chain | ✅ couvert | `npm audit --omit=dev` (conteneur) + `composer audit` + `pip-audit` (conteneur, root) EXÉCUTÉS : **0 vuln ×3** |
| Cybersécurité — surface d'attaque | ✅ couvert | A1–A21 re-verdictés sur la surface neuve + **2 lignes neuves A22/A23** ; 3 points contre-vérifiés (dont `default_workflow_permissions` lu via l'API GitHub) |
| Infra / Mercure | ✅ couvert | hub dev non-anonyme, CORS borné, 127.0.0.1, **image épinglée v0.24.2** (Mercure 1.0 du 16/09 casse les tokens 0.x) ; prod v0.19 + `MercureHardeningTest` |
| Prod-readiness / observabilité | ✅ couvert | Sentry câblé (3 DSN) ; sauvegarde nocturne + **crochet hors site implémenté** (`DatabaseBackupCommand`, `BACKUP_SYNC_COMMAND`) ; déploiement avec sauvegarde pré-migration obligatoire ; 11 healthchecks dev, 21 limites/healthchecks prod ; **dev : aucune politique de redémarrage** (le worker est mort 5 h le 17/09, PR #918) |
| RGPD | ✅ couvert (statique) | 9 commandes de purge + export + effacement ; **trou confirmé : `opponent_travel` n'est purgé ni au reset de saison ni à l'effacement du club** (BCK-24, Élevée) |
| Performance solveur | ✅ couvert | **Behat complet EXÉCUTÉ** sous sandbox : 60/60 scénarios, génération de saison COMPLETED, overlay COMPLETED, reprise COMPLETED — **433 s de mur pour la suite** (le smoke-solver historique est remplacé par la feature Gherkin depuis P4-165 : le score n'est plus comparable au 21640 du 08-27) |
| Alignement contraintes 3 couches | ✅ couvert | table complète 20 cellules × 3 couches ; parité génération⇄verdict relue ; rail `/place-matches` champ par champ ; **1 scission de promesse Élevée** (ALIGN-11) |
| Restauration de backup | ⬜ non couvert cette édition | drill exécuté le 08-27 (parité 66 tables / 91 policies) ; non rejoué — le chemin de code n'a pas changé (`DatabaseBackupCommand`, runbook) |
| **Réversibilité des migrations / retour arrière de déploiement** | 🟡 partiel — **NOUVEL AXE CANDIDAT** | 148 migrations, **148 avec `down()`**, 5 déclarées irréversibles avec justification ; 51 avec `DROP TABLE` en `up` ; déploiement = sauvegarde pré-migration **obligatoire** (`scripts/remote-deploy.sh:68-80`) puis migrate ; **aucun retour arrière automatisé**, aucun `down()` jamais exercé en CI (le chemin de secours = restauration de la sauvegarde). Rendu visible et redevable ; à couvrir par un drill « migrate → down → up » sur base temporaire |
| UX-Cohérence | ✅ couvert | collecte statique exhaustive (primitives, tokens, terminologie — commandes reproductibles) |
| UX-Simplicité / Intuitivité | ✅ couvert | proxys statiques (4 flux-clés comptés) + **parcours réel** (Calendrier, Conflits, Importer, Configuration, Semaine type, planning, cockpit, club, profil) sur les données du club pilote |
| Inclusivité-a11y | ✅ couvert (**l'axe passe de partiel à couvert**) | **axe-core EXÉCUTÉ** (wcag2a/aa/21aa) sur 11 écrans × 2 thèmes + 2 trails clavier réels (62 Tab, 0 cible sans anneau) + piège de focus d'une modale (12/12 dans le dialog, Échap ferme, focus rendu) ; toujours pas de lecteur d'écran |
| Coûts / scalabilité financière | ⬜ non couvert (pas de données réelles) | ligne permanente — aucune donnée de facturation/infra prod ; le harnais `backend/scripts/load-test/` existe toujours et n'a toujours pas nourri l'axe |

---

## Synthèse des notes

| Critère | 2026-08-19 | 2026-08-27 | **2026-09-18** |
|---|---|---|---|
| 1. Documentation | 89 | 90 | **82** |
| 2. Pertinence du besoin | 92 | 93 | **92** |
| 3a. Code backend | 82 | 84 | **72** |
| 3b. Code engine | 91 | 87 | **85** |
| 3c. Code frontend | 86 | 86 | **84** |
| 4. Supply chain | 96 | 96 | **96** |
| 5. Performance solveur | 90 | 90 | **88** |
| **État global (pondéré)** | 87 | 87 | **82** |

Pondération inchangée : doc 10 % · besoin 10 % · backend 25 % · engine 20 % · frontend 15 % · supply 5 % · perf 7,5 % · UX 7,5 %.
Calcul = 82·.10 + 92·.10 + 72·.25 + 85·.20 + 84·.15 + 96·.05 + 88·.075 + 72·.075 = **81,8** → **82**. Malus transversal 0.

⚠ **Première baisse de la série (87 → 82)**, et elle est voulue : le barème mesure la distance au commercialisable, pas l'énergie dépensée. Le delta est le plus gros de l'histoire du dépôt (243 commits, +12 120 lignes backend) et le produit a **beaucoup** gagné en valeur d'usage (la passe de tests du fondateur a livré sept PR en trois jours). Mais trois choses ont grandi avec lui sans garde : la **duplication** (le chargeur de radar copié et déjà divergent, BCK-23), la **liste tenue à la main** là où le dépôt sait faire des méta-gardes (la purge, d'où BCK-24 Élevée), et la **promesse d'écran qui dépasse l'effet** (ALIGN-11 Élevée, sur le wizard de génération, le flux le plus ancien du produit). Deux Élevées confirmées à la main contre une le 08-27.

### Score UX (axe additif — noté À PART, sévérité extrême)

| Sous-axe | 08-19 | 08-27 | **09-18** | Plafond appliqué |
|---|---|---|---|---|
| UX-Cohérence | 84 | 75 | **80** | UXC-18/19 corrigés ; résidus Faibles seulement (UXC-20/21/23/24) → aucun plafond, mais 4 Faibles neufs |
| UX-Simplicité & Intuitivité | 80 | 75 | **75** | **UXS-08 (Moyen confirmé)** → plafond 75 ; UXS-05 corrigé sur le Calendrier, le motif s'est déplacé sur Configuration/Semaine type |
| Inclusivité / a11y | 78 | 80 | **72** | **A11Y-22 (Moyen, MESURÉ en navigateur : 2,56 et 4,13)** → plafond 75 ; + A11Y-23/24 (Faibles, axe) ; clavier et modale exemplaires en réel |
| **Score UX général** | 78 | 75 | **72** | = le PLUS BAS des sous-axes couverts |

**Lecture rapide.** L'édition de la **surface qui déborde ses gardes**. Le registre de l'édition précédente a mordu comme jamais : **ENG-36/37/38 fermés par un registre de parité AST** (la reco P1, livrée au-dessus du cahier), **UXC-18/19, UXS-05/06, A11Y-17/18/19, FRT-27/29, DOC-34/35/36 corrigés avec preuve** — 19 findings soldés. Le frontend passe **3024 tests** (+840 en 22 jours) verts au premier run, l'axe a11y devient couvert grâce à un vrai scan dynamique, la posture cyber tient sur une surface qui a encore doublé (2 tables fédérales partagées, 6 routes custom). Et pourtant la note baisse, parce que deux Élevées naissent là où l'on ne regardait plus : le **wizard** (« préfère ce gymnase » en Obligatoire *réserve* le gymnase à 3 équipes et le *bannit* pour les 20 autres, sans un mot à l'écran — ALIGN-11) et l'**effacement RGPD** (la promesse « seule l'identité FFBB survit » est fausse à la lettre depuis le 28/08 — BCK-24). Les deux ont la même cause : une **liste tenue à la main sans méta-garde** (les exemptions du wizard, les entités de la purge), dans un dépôt qui a prouvé qu'il sait écrire ces gardes (`TenantOwnedInterfaceCompletenessTest`, `RgpdExportCompletenessTest`, `test_hard_layer_parity_registry`).

---

## Registre des findings

### Findings de l'édition précédente — statuts

| ID | Titre | Zone | Gravité | Vérif | **Statut** |
|---|---|---|---|---|---|
| **ENG-36** | `travelTime` absent du verdict | engine | Élevée | contre-vérifié | **corrigé + MÉCANISÉ** — `venue_travel_times` passé à `_apply_hard` (`validate_assignments.py:485`), miroir `_travel_time_move_violation` (`:254-341`), `tests/test_validate_travel_time.py` ; **registre de parité** `engine/tests/test_hard_layer_parity_registry.py` (455 l.) : diff AST bidirectionnel des familles HARD **et** des arguments, exceptions nominatives gardées contre la péremption, `DECLARED_ASYMMETRIES = {}` — la reco P1 du 08-27 livrée au-dessus du cahier |
| ENG-38 | `venue_minimums` absent du verdict | engine | Faible | contre-vérifié | **corrigé** (P4-152) — `add_venue_minimum_constraints` en `validate:495` + `_venue_minimum_move_violation` (`:343-418`) + test dédié ; l'exemption a quitté la carte des asymétries |
| ENG-37 | Géométrie trajet dupliquée diagnostic vs pose | engine | Faible | confirmé | **corrigé** — `result_builder/diagnostics.py:27,1070` consomme `is_travel_too_tight` (source unique `travel.py`), test-garde nommé |
| ENG-39 | `result_builder.py` 1931 / `objective.py` 1524 | engine | Info | confirmé | **partiel** — les deux sont devenus des paquets (4 modules chacun) ; résidu `result_builder/diagnostics.py` **1614 l.**, `validate_assignments.py` 1008, `main.py` 948, `objective/terms.py` 940 |
| ENG-34 | Déterminisme 8 workers > 200 | engine | Info | confirmé | **inchangé, assumé** (`main.py:441-447`), renforcé par `test_deterministic_term_order.py` |
| BCK-19 | God-services (2846/2285/1445/1413, 44 fichiers > 300) | backend | Moyenne | `wc -l` re-exécuté | **partiel** — un seul des cinq démembré : `CustomRoutesOpenApiFactory` **2 846 → 90 l.** (19 `PathContributor/*`, plus gros 444) ; les quatre autres ont grossi (`BcclSeeder` **2 998** +713, `FbiFixtureImporter` **1 849** +404, `ScheduleConstraintBuilder` 1 487, + entrants `MatchConflictDetector` **1 157**, `CalendarEntryStateProcessor` **1 060**) ; fichiers > 300 l. : 44 → **54** |
| BCK-18 | Provenance absente de l'échéance partagée | backend | Faible | confirmé | **ouvert, décision réaffirmée** — doctrine « un compte, jamais un qui » généralisée aux tables partagées neuves ; correctif connexe livré : `Version20260828170000` pose le `REVOKE DELETE` que le GRANT n'avait jamais retiré |
| BCK-20 | PUT partiel `VenueTravelTimeInput` | backend | Mineure | confirmé | **ouvert** (`Dto/VenueTravelTimeInput.php:28-35`) |
| BCK-21 | `/validate-impact` sans test nommé | backend | Mineure | confirmé | **partiel** — `ValidateImpactApiTest.php` existe (membre, 404 cross-club) ; le 400 « sans club » reste sans test |
| BCK-22 | Rafale IGN synchrone | backend | Faible | confirmé | **partiel, déplacé** — budget global 30 s posé (`IgnRoutingClient::BATCH_BUDGET_SECONDS`) ; le nouvel orchestrateur `/api/opponents/refresh` rouvre le pire cas par appels unitaires → BCK-32 |
| FRT-27 | Matrice de trajet sans `onError` | frontend | Moyenne | confirmé | **corrigé** — `wizard/queries.ts:171-186` toast sur create/update (commentaire FRT-27), autofill géré au call-site ; **note** : un filet global `MutationCache.onError` (`shared/lib/queryClient.ts:26-36`) toaste déjà toute mutation sans `onError` — l'édition 08-27 ne l'avait pas vu, la gravité initiale était surévaluée |
| FRT-28 | Types manuels des API neuves sans filet | frontend | Faible | confirmé | **partiel** — `TsFieldsMatchOpenApiSchemaTest` garde **8 paires** (Team ×3, VenueTravelTime, SharedTrainingBlock, 3 routes) sur **216 interfaces manuelles** (3,7 %) |
| FRT-29 | 6 warnings react-refresh | frontend | Info | exécuté | **corrigé** — eslint : sortie vide |
| FRT-20 | Hook-mocking | frontend | Faible | confirmé | **ouvert, s'étend** — 63 fichiers `vi.mock("*queries*")` vs 41 au patron API ; `msw` déclaré, jamais importé (→ FRT-35) |
| FRT-21 | Cycles inter-features sans garde | frontend | Faible | confirmé | **ouvert, grossi** — wizard→matches ×8 (neuf), matches⇄planning, cockpit→planning ×9 ; `useVenues/useTeams/useCoaches` définis deux fois (`matches/queries.ts:107-139`, `planning/queries.ts:158-175`) |
| FRT-22 | Team/Venue/Coach ×3 | frontend | Faible | confirmé | **partiel (amélioré)** — `level: TeamLevel \| null` corrigé (`matches/api.ts:352`) ; toujours 3 copies |
| DOC-34 | Meta-changelog OpenAPI 61 Ko | doc | Moyenne | `wc -c` re-exécuté | **corrigé** — **8 784 octets**, règle « 8 dernières entrées » écrite dans le fichier (`openapi-snapshot.meta.md:12`) |
| DOC-35 | Stamp-fleuve horizontal | doc | Faible | confirmé | **corrigé** — `engine-inventory.md:3-12` = 800 octets, une passe courante |
| DOC-36 | CLAUDE.md §2 omet `/validate-assignments` | doc | Mineure | confirmé | **corrigé dans CLAUDE.md** (`:23`) — **mais pas la règle** : la même omission survit dans `docs/project-map.md:133` → DOC-37 |
| DOC-33 | Stamps-fleuves | doc | Faible | confirmé | **corrigé** (0 stamp empilé sur 38 fichiers, stamp le plus vieux à 3 jours) |
| UXC-18 | 4 mots pour « litige » | ux | Moyenne | confirmé | **corrigé** — un seul mot visible, « Conflits » (onglet, carte, chips, renvois) ; « litige »/« signalement » n'existent plus qu'en commentaire |
| UXC-19 | Dates ISO brutes | ux | Moyenne | confirmé | **corrigé** — `PlacementPanel.tsx:164`, `UnplacedList.tsx:53` (`frDateWeekdayNoYear`) |
| UXS-05 | Échec de chargement rendu comme vide (MatchesPage) | ux | Moyenne | confirmé | **corrigé sur le Calendrier** (`CalendarPage.tsx:430-446` : spinner / `LoadErrorHint` + retry ×3, test NR) — **le motif s'est déplacé** sur Configuration et Semaine type → UXS-08 |
| UXS-06 | « Contraintes ✓ » sur club vierge | ux | Faible | confirmé | **corrigé** — `WizardLayout.tsx:133-139`, tag `UXS-06` dans le code |
| UXC-10 | Empty states inline | ux | Faible | confirmé | **ouvert, réduit 17 → 4** (tous « aucun résultat de filtre », défendables) |
| UXC-12 | Console superadmin hors design system | ux | Faible | confirmé | **corrigé dans son geste** — 563 → **5** couleurs brutes + **garde automatique** `consolePalette.guard.test.ts` ; résidu assumé : 205 `white/black` hors garde |
| A11Y-16 | `text-[10px]` généralisé | ux | Info | confirmé | **ouvert, stable** — 30 occ./10 fichiers, grilles denses (assumé) ; 2 hors grille (`slotFields.tsx:74`, `ConflictLine.tsx:277`) |
| A11Y-17 | `AlertTriangle` sans nom | ux | Faible | confirmé | **corrigé** (`WeekendGrid.tsx:270` `aria-label="Hors fenêtre ligue"`, tag A11Y-17) |
| A11Y-18 | ✅ seul | ux | Faible | confirmé | **corrigé** (« ✅ validée », `DayDialog.tsx:501`, `RadarPanel.tsx:585`) |
| A11Y-19 | Lock sans aria-hidden / verrou sans pourquoi | ux | Faible | confirmé | **corrigé (a11y)** — `step-rail.tsx:74` `aria-hidden`, `:59` « étape verrouillée » ; le POURQUOI reste non dit, par choix documenté → résidu Info |
| ALIGN-10 | MIN_SESSIONS cible soft, jamais un plancher | align | Faible | **confirmé au code** | **ouvert — et désormais piégé** : `main.py:516-532` passe des zéros ; l'exemption du registre de parité le couvrirait par avance (voir ENG-41) |

**Bilan reprise : 19 corrigés/fermés** (ENG-36/37/38, FRT-27/29, DOC-33/34/35/36, UXC-12/18/19, UXS-05/06, A11Y-17/18/19, + BCK-18 connexe) · 8 partiels (ENG-39, BCK-19/21/22, FRT-22/28, DOC-36 règle, UXS-05 déplacé) · 8 ouverts (ENG-34, BCK-18/20, FRT-20/21, UXC-10, A11Y-16, ALIGN-10) · **aucune régression** sur un finding corrigé.

### Nouveaux findings (cette édition)

| ID | Titre | Zone | Gravité | Vérif | Statut |
|---|---|---|---|---|---|
| **ALIGN-11** | **« Préfère ce gymnase » + Obligatoire = impose ET réserve le gymnase au groupe, sans un mot à l'écran.** Le wizard émet `preferredVenueId` avec `ruleType: HARD` et nomme la règle « Groupe X · préfère Gymnase Y » (`ConstraintsStep.tsx:384-386`, sélecteur « Obligatoire » `:47`) ; l'engine traite un `preferredVenueId` HARD comme un gymnase **forcé** (`parsing.py:315-331`) ; le backend, pour toute cible de groupe en HARD, ajoute **N interdictions `forbiddenVenueId` HARD pour chaque équipe hors du tag** (`ScheduleConstraintBuilder.php:1368-1388`, « interdit hors tag »). 0 occurrence de « exclusi », « réserv » ou « autres équipes » dans l'écran. Un gestionnaire peut bannir un gymnase pour 20 équipes en croyant exprimer une préférence pour 3 — sur le flux de génération | align/front | **Élevée** | **confirmé (3 couches relues à la main)** | nouveau |
| **BCK-24** | **`opponent_travel` n'est purgé par aucun purger : l'effacement RGPD d'un club laisse ses données en base.** Entité `TenantOwnedInterface` (`Entity/OpponentTravel.php:51`, tenant depuis `Version20260828120000`), **absente** de `SeasonDataPurger::purge()` (`:110-172`) et de `ErasedClubPurger::purge()` (`:63-70`) — 0 occurrence dans les deux fichiers (re-grep). Survivent après effacement : le code organisme de chaque adversaire, le gymnase choisi, son libellé, ses coordonnées et les minutes de trajet **depuis le siège du club** ; « seule l'identité FFBB survit » (`ErasedClubPurger.php:82-85`) est faux à la lettre. Cause racine : l'export est dérivé dynamiquement du marqueur (`RgpdExportCompletenessTest`), la purge est une liste tenue à la main **sans méta-garde** | backend | **Élevée** | **confirmé (re-grep, 45 entités tenant croisées)** | nouveau |
| **BCK-23** | **Le gardien « nouveau depuis ta dernière visite » ne voit pas le même radar que l'écran.** `MatchModuleDeltaComputer::currentConflicts()` appelle `detect()` sans `profilesByTeam` ni `roundTripByFixtureId` (`:161-174`, seuls `clubToday:` et `playerMemberships:` passés) là où `FixtureConflictsController` les passe (`:306-307`) → le détecteur retombe sur le profil de durée par défaut et un trajet nul : tout conflit qui n'existe que par le trajet ou un profil club **n'entre jamais** dans `newConflictFingerprints`, le badge « Nouveau » sous-déclare en silence. Le docblock `:38-41` affirme « chargé exactement comme FixtureConflictsController » (faux) ; `MatchVisitDeltaParityTest` (13 cas) **n'appelle jamais `/api/fixtures/conflicts`** et ne teste donc pas la parité que son nom promet. Recoupe P4-211 (ouvert en roadmap le 17/09) | backend | **Moyenne** | **confirmé (deux appels lus côte à côte)** | nouveau |
| **ENG-41** | **L'unique exemption du registre de parité repose sur un fait faux et désarme le garde pour le jour d'ALIGN-10.** `DECLARED_ARG_DIVERGENCES["min_sessions_by_team"]` (`test_hard_layer_parity_registry.py:376`) justifie « /generate passe les minimums réels, le verdict des zéros » — or `main.py:516-532` initialise `adjusted_min_by_team` à **0 pour chaque équipe** et ne l'écrit ensuite qu'à 0 : les deux côtés passent des zéros identiques. Le garde compare le **texte source** des expressions, pas les valeurs, donc l'exemption reste verte ; le jour où MIN_SESSIONS devient un plancher dur, le verdict continuera de passer des zéros et **le registre ne rougira pas** — exactement la classe ENG-36 | engine | **Moyenne** | **confirmé (main.py:516,529,532 lus)** | nouveau |
| **ENG-42** | **Une contrainte de famille/type inconnue est droppée sans diagnostic gestionnaire** — seule branche muette du parseur : `parsing.py:451-462` fait `logger.warning` et rien d'autre, quand les 6 autres branches « non honorée » alimentent `parse_warnings`. Le garde de contrat est MAJOR-only : un backend 2.22 devant un engine 2.21 déclenche ce cas (skew de déploiement). Atténuation : `ConstraintKeysAreHonouredByEngineTest` rend le scénario improbable **en CI** | engine | **Moyenne** | **confirmé** | nouveau |
| **ENG-40** | **`/place-matches` : le temps de CONSTRUCTION du modèle n'est borné par rien, sur le seul rail synchrone.** `max_time_in_seconds` n'est posé qu'après le build (`match_placement.py:434`) ; le build est quadratique (paires de candidats `:349-351`, `_overlap_pairs` `:318-334`, passerelles `:359-386`) ; cap `MAX_MATCHES = 2000` ; le backend n'horizonne pas (`MatchPlacementPayloadBuilder.php:113-123` itère toute la saison) ; transport 60 s ; **aucun gate perf** sur ce rail (`tests/perf/test_perf_dense.py` = `/generate` seul). **Non mesuré** (gravité par lecture) | engine | Moyenne | confirmé (lecture) — **non mesuré** | nouveau |
| **FRT-30** | **`setState` du parent pendant le rendu de `DiagnosticsPanel`** : `DiagnosticsPanel.tsx:122-127` appelle `onHighlight(new Set())` (= `highlightSlots`, setState de `PlanningPage.tsx:1770`) dans le corps du rendu ; React l'avertit à chaque run (« Cannot update a component (`PlanningPage`) while rendering a different component ») ; le commentaire `:101-104` explique avoir contourné le lint `set-state-in-effect` | frontend | **Moyenne** | **confirmé (log vitest + code lu)** | nouveau |
| **FRT-31** | **Les gardes de placement d'un match sont front-only et reposent sur des lectures non gardées** : 14 des 17 lectures de `CalendarPage` passent par `?? []` (`:162,566-567`…) ; `lib/matchAccess.ts:49-50` traite 0 fenêtre comme « rien à imposer » → une lecture ÉCHOUÉE (toast global unique) est indistinguable d'un club sans créneau : `PlacementPanel.tsx:134-148` rend `canPlace=true` et le gestionnaire place dans un gymnase fermé ; `FixtureStateProcessor.php` ne vérifie côté serveur ni fenêtre d'accès, ni indisponibilité, ni enveloppe ligue (le conflit n'apparaît qu'a posteriori au radar). Le « vide crédible » que `readState.ts:12-16` documente | frontend | **Moyenne** | confirmé | nouveau |
| **FRT-32** | **Miroir silencieux non déclaré : l'enveloppe ligue.** `matches/lib/envelope.ts:38-69` redérive `MatchConflictDetector::leagueWindowViolations` (`:560-590`) pour marquer `outOfEnvelope` sur la grille (alors que `LEAGUE_WINDOW_VIOLATION` est déjà servi dans `allConflicts`) **et décider** `envelopeBlocked → canPlace` (`PlacementPanel.tsx:132,148`) ; absent de `FrontRederivationRegistryTest`, sans test de parité — régime (3) interdit par `.claude/rules/frontend.md` | frontend | **Moyenne** | confirmé | nouveau |
| **UXS-08** | **Échec de chargement rendu comme « rien à configurer » (UXS-05 déplacé).** `ConfigurationPage.tsx:60-66` ouvre 7 queries consommées en `?? []` (`:90-111`), 0 `readState`/`LoadErrorHint` : si `useVenues` échoue, `EmptyHint` `:160` affiche « Aucun gymnase n'accueille de matchs — ajoutez un accès », conseil faux et actionnable ; `TypicalWeekPage.tsx:23-28,49-59` idem (grille « Semaine type » vide = club sans habitudes) | ux | **Moyenne** | confirmé | nouveau |
| **A11Y-22** | **Texte à opacité réduite sous le seuil AA, clair ET sombre — MESURÉ en navigateur.** axe : cases d'habitude de la grille de week-end `text-[10px] text-muted-foreground` sous `opacity-60` → **2,56** (clair) / **2,97** (sombre) ; badge « conflits à traiter » `text-warning` sur `bg-warning/15` (`ConflictRadar.tsx:48`) → **4,13** en clair ; grille de planning sombre `text-[10px]` → **4,33** (×10 nœuds) ; `/club` clair `text-accent` → 4,11. Calcul statique concordant sur 11 sites `text-*/NN` (`WeekGrid.tsx:321` `/60` = 2,57 ; `WeekendGrid.tsx:274` `/80` = 3,83). WCAG 1.4.3 ne fait pas d'exception pour le texte secondaire non décoratif | ux | **Moyenne** | **confirmé (axe EXÉCUTÉ, 2 thèmes)** | nouveau |
| **DOC-45** | **Les poids du gymnase préféré/évité sont faux dans 2 docs de référence sur 3** : `engine/docs/constraint-vocabulary.md:71-72` et `frontend/docs/constraint-emission.md` (table d'alignement) annoncent **+60/−60** ; le code vaut **+10/−10** depuis le rebalancement V10 du 2026-08-15 (`objective/weights.py:53,61`). `backend/docs/constraint-coverage.md:57` seul est juste. **34 jours et trois stamps « re-confronté au code »** ont passé au-dessus (la rotation vérifie l'existence des symboles, pas leurs valeurs). C'est un chiffre d'arbitrage produit | doc | **Moyenne** | **confirmé (weights.py lu)** | nouveau |
| **DOC-37** | `docs/project-map.md` §3 ampute le moteur de 2 endpoints sur 6 (`:133` omet `POST /place-matches` et `/validate-assignments`, `main.py:854,874`) et annonce `result_builder.py` fichier là où c'est un paquet (`:145`, qui omet aussi `match_placement.py`, `validate_assignments.py`, `compromise.py`). DOC-36 a corrigé le cas dans CLAUDE.md, pas la règle dans le doc n°2 de l'ordre de lecture | doc | Moyenne | confirmé | nouveau |
| **DOC-38** | `specs/courantes/module-matchs.md` = **3 093 lignes** (1 203 au 08-27, +157 % en 3 semaines), 28 titres `##` datés d'une PR : un journal chronologique là où `specs/README.md` demande l'état courant ; son stamp doit énumérer 11 sections non re-sondées ; « que fait l'onglet Conflits aujourd'hui ? » demande de réconcilier 6 sections datées. DOC-34 réincarné à la plus grande échelle du dépôt | doc | Moyenne | `wc -l` re-exécuté | nouveau |
| **DOC-39** | Le garde `test_contract_version_doc_sync.py:37-50` liste 9 docs ; **4 docs citent le contrat actif hors liste** (`backend/docs/generation-flow.md:134,161`, `specs/courantes/generation-pipeline.md:10`, `backend/docs/geo-api.md:11`, `module-matchs.md:58,312`) et **3 ont réellement dérivé** (2.20 → 2.21) dans le delta, rattrapés à la main par la rotation du 15/09 et non par la CI — le docstring du garde raconte déjà l'épisode D-37 | doc | Moyenne | confirmé | nouveau |
| **SEC-19** | **`POST /api/opponents/travel/manual` sans limiteur dédié** — la seule route à rafale du module qui n'en a pas (`OpponentTravelController.php:87-123` vs `:192` sur `/resolve`) : chaque appel = 1 FFBB + 1 IGN + jusqu'à 2 mutations du compteur communautaire ; borne effective `api` 300/min/user ≈ 600 appels tiers/min. Corollaire : le compteur partagé est gonflable par un seul club (ligne CLUB + une par `teamKey`) | backend/cyber | **Moyenne** | **confirmé (méthode relue en entier, aucun limiteur)** | nouveau |
| **A11Y-23** | Primitive `Tabs` : `aria-controls` pointe vers un panneau non rendu pour les onglets inactifs (`shared/components/ui/tabs.tsx:116,151` — seul le panneau actif est dans le DOM) → axe `aria-valid-attr-value` (impact « critical » axe) sur « Semaine type », 2 thèmes | ux | Faible | **confirmé (axe EXÉCUTÉ)** | nouveau |
| **A11Y-24** | Grille « Semaine type » : région défilante `.overflow-auto` sans `tabindex` → inatteignable au clavier (axe `scrollable-region-focusable`, 2 thèmes) | ux | Faible | **confirmé (axe EXÉCUTÉ)** | nouveau |
| **FRT-37** | **Une réponse inattendue d'une requête secondaire abat toute la route** : `ModuleVisitBanner` → `moduleVisitSummary` → `visitDeltaSegments` (`:6`) lit `.length` sans validation de forme ; une réponse `{}` de `POST /api/matches/module-visit` plante le Calendrier entier (« Arrêt de jeu imprévu ») — constaté en réel pendant le parcours (réponse simulée), l'API réelle rend toujours la bonne forme. Le bandeau devrait dégrader seul, pas l'écran | frontend | Faible | **confirmé (reproduit en navigateur)** | nouveau |
| SEC-20 | Empoisonnement par appariement de l'annuaire de salles partagé : contenu fédéral (re-résolu serveur, #904 étanche) mais **association code organisme ↔ salle choisie par le club**, ligne MANUAL permanente (GRANT sans DELETE, `Version20260915140000:85`) et sans provenance, servie à tous les pairs — même angle mort que BCK-18 | backend/cyber | Faible | confirmé | nouveau |
| SEC-21 | `permissions:` absent de `ci.yml` et `security-weekly.yml` (`deploy.yml`/`mirror-images.yml` l'ont) — **rétrogradé Mineure après contre-vérification** : `default_workflow_permissions = read` au niveau du dépôt (API GitHub), le jeton hérité est en lecture seule | infra | Mineure | **contre-vérifié (API GitHub)** | nouveau |
| SEC-22 | Import FBI xlsx sans borne propre : acceptation sur mime **OU** extension (`FixtureImportGate.php:74-88`), aucune taille max applicative, aucun cap de lignes, feuille entière matérialisée (`FbiFixtureImporter.php:1646-1647`) ; bornes réelles = nginx 20 Mo + `memory_limit` 192 M ; **XXE réfutée** (reader épinglé `READER_XLSX`) | backend/cyber | Faible | confirmé | nouveau |
| BCK-26 | Résidu de texte club dans la table globale `opponent_directory` : `OpponentLocationResolver::locateCity():354` `?? $name` écrit `Fixture::getOpponentLabel()` (saisi par le club) en repli — quasi mort (le hit est trouvé PAR le nom), mais invariant ni tenu ni falsifié (`OpponentDirectoryShareTest` : 0 cas sur l'origine fédérale, contre 1 explicite côté suggestions) | backend | Faible | confirmé | nouveau |
| BCK-27 | Aucun `Assert\Length` dans 26 DTO d'entrée (`VenueInput.php:29` en a un) : un libellé > 180 car. traverse `FixtureStateProcessor.php:90` → `Entity/Fixture.php:76` → PostgreSQL `value too long` → **500** au lieu d'un 422 parlant | backend | Faible | confirmé | nouveau |
| BCK-32 | Pire cas non borné sur `POST /api/opponents/refresh` : cap 200 et limiteur posés, mais passes (a)/(b) à appels unitaires non budgétés (IGN 5 s par groupe jusqu'à 200, FFBB 8 s par code) — minutes de mur dans une requête synchrone ; et l'orchestrateur laisse passer 200 adversaires à un service dont la route dédiée en cappe 60 (`OpponentTravelResolver.php:52`) — BCK-22 déplacé | backend | Faible | confirmé | nouveau |
| ALIGN-12 | **Le solveur de placement ignore le trajet adversaire que le radar modélise** : `MatchConflictDetector` allonge l'empreinte AWAY du trajet aller-retour (`:281-287`) ; `/place-matches` ne reçoit aucune donnée de trajet (`MatchPlacementPayloadBuilder.php:214-230`, 0 hit `travel` dans `match_placement.py`) → « Placer automatiquement » peut poser un domicile que le radar signale immédiatement — sens inverse (dangereux) de P4-215, **non documenté** | align/engine | Moyenne | confirmé | nouveau |
| ALIGN-13 | `matches[].kickoffEstimated` transporté, déclaré au schéma (`match_input_schema.py:113`), **jamais lu** par le solveur : une heure AWAY devinée pèse `W_COACH_MAIN=60` comme une heure certaine — « accepté mais non consommé », la forme d'avant ENG-36 | align/engine | Faible | confirmé | nouveau |
| ALIGN-14 | `ConstraintKeysAreHonouredByEngineTest` prouve la CLÉ, pas la CELLULE (clé × ruleType) : les crans SOFT de `minStartTime`/`maxStartTime`/`forbiddenDays`/`forbiddenVenueId` ne sont prouvés par aucun test cross-stack — précisément le motif « soft = placebo » ENG-10/11/12 | align/tests | Faible | confirmé | nouveau |
| ALIGN-15 | Le gate `forcedDays` (`PreSolvePreventionWarnings:315-345`) ne lit que `teams[].forcedVenueId`, ignore une FACILITY `forcedVenueId`/`allowedDays` combinée — bloque moins, jamais plus | align/backend | Faible | confirmé | nouveau |
| ENG-43 | Le snapshot persisté n'est pas le payload envoyé : `GenerateScheduleHandler.php:253,266` greffent `withPreviousAssignments`/`withSocleReferenceAssignments` **après** `setSnapshotData` (`:234`), rien ne conserve la version finale en prod (`DevScheduleReportWriter` dev-only) → un incident n'est pas rejouable à l'identique | engine/backend | Faible | confirmé | nouveau |
| ENG-44 | Annotations de schéma périmées (« ACCEPTÉ mais NON consommé » sur `sharedBlocks`/`teamLinks`, `validate_input_schema.py:83-90`, `input_schema.py:28`) alors que le verdict les consomme — ce sont celles sur lesquelles l'audit 08-27 s'est appuyé ; + défauts `version` `"2.0"`/`"2.4"` non alignés (`input_schema.py:314`, `validate_input_schema.py:61`) | engine | Faible | confirmé | nouveau |
| ENG-45 | `constraints[]` sans `max_length` dans `input_schema.py:326` et `validate_input_schema.py:69` ; `teams[].leagueWindows` idem (`match_input_schema.py:80`) — contre la doctrine A10 écrite en tête des mêmes fichiers | engine | Faible | confirmé | nouveau |
| ENG-46 | bandit configuré (`pyproject.toml:105`), annoncé (CLAUDE.md §3), **jamais gaté en CI** (0 hit dans `.github/workflows/*`) ; atténué par semgrep `p/python` + `p/security-audit` (required) | engine | Info | confirmé | nouveau |
| ENG-47 | Message `unused_slot` mort à l'arrivée en anglais (`diagnostics.py:1321`) reconstruit systématiquement côté backend (`DiagnosticMessageBuilder.php:39-48`) — deux propriétaires | engine | Info | confirmé | nouveau |
| BCK-28 | Six `catch (Throwable) {}` strictement vides sans une ligne de log (`AuthController.php:799,836,868,886`, `AdminFeedbackController.php:306`, `FeedbackController.php:187`) : un Redis indisponible avale les mails de vérification en silence total — logger côté serveur ne fuite rien au client | backend | Mineure | confirmé | nouveau |
| BCK-29 | `GenerateScheduleHandler` sans garde de statut terminal (`:95-127`) : worker tué entre commit et ack → re-solve complet d'un planning déjà livré | backend | Info | confirmé | nouveau |
| BCK-30 | `SYMFONY_DEPRECATIONS_HELPER = "weak"` (`phpunit.xml.dist:42`) : aucune dépréciation ne fait jamais échouer la suite sur une stack épinglée LTS | backend | Info | confirmé | nouveau |
| BCK-31 | N+1 hydraté dans l'importeur FBI : `findBy` par compétition pour un simple comptage (`FbiFixtureImporter.php:381-386`) | backend | Mineure | confirmé | nouveau |
| FRT-33 | Monolithes du module matchs : `matches/api.ts` **1 316 l.** (63 interfaces), `queries.ts` 757, `CalendarPage.tsx` **645** (17 lectures + 3 mutations + 45 `useMemo`), `WeekWorkbench` 28 props ; 31 fichiers prod > 400 l. | frontend | Faible | `wc -l` | nouveau |
| FRT-34 | 70 avertissements `act(...)` par run (23 sur `ConstraintsStep.test.tsx`) — bruit qui masque le seul avertissement réel (FRT-30) ; 45 `as unknown as`/`as never` en tests | frontend | Info | exécuté | nouveau |
| FRT-35 | `msw` déclaré (`package.json:63`), 0 import ; `docs/frontend-strategy.md:6,101` décrit un patron « mock réseau » qui n'existe pas (le réel = `vi.mock("./api")`) | frontend/doc | Info | confirmé | nouveau |
| FRT-36 | Chemin d'échec non testé sur la page neuve : `ConflictsPage.test.tsx` n'exerce jamais `readFailed` ; 5 tests d'erreur sur 73 dans `matches/` ; `WeekWorkbench.tsx` (365 l.) et `CalendarControls.tsx` sans test frère | frontend | Faible | confirmé | nouveau |
| UXC-20 | Patron « puce `Button` + `aria-pressed` + compteur » recodé **6 fois / 2 fichiers** (`CalendarControls.tsx:117,175,199` · `ConflictsPage.tsx:397,433,473`), les deux blocs « Familles » verbatim identiques ; 3 variantes de plus ailleurs — aucune primitive `FilterChip` (roadmap P4-216 sous-comptait à 4) | ux | Faible | confirmé | nouveau |
| UXC-21 | `FilterToggle` a **1** consommateur ; ses 2 copies locales d'origine survivent byte-pour-byte (`ReviewQueue.tsx:231-247` vs `filter-toggle.tsx:11-17`), signalées dans `.claude/rules/frontend.md`, non traitées | ux | Faible | confirmé | nouveau |
| UXC-23 | « salle » pour désigner le gymnase DU CLUB à 4 sites (`ValidateDialog.tsx:69`, `VenueLabelsSection.tsx:96,197`, `MatchDurationsEditor.tsx:197` — les deux mots dans une phrase), contre la règle interne `AUD-UXC-13` | ux | Faible | confirmé | nouveau |
| UXC-24 | Un seul statut `UNPLACED`, trois mots selon l'onglet du même écran : « Importé » (Mois/Phase, `fixtureStatusLabel.ts:18`), « À confirmer » (Semaine, `WeekendGrid.tsx:215`), « à placer » (`WeekWorkbench.tsx:332`) — sous-découpage réel et documenté en commentaire, jamais expliqué au gestionnaire | ux | Faible | confirmé | nouveau |
| UXC-22 | Chargement rendu par la primitive de vide (`OpponentTravelCard.tsx:151` `<EmptyHint>Chargement…</EmptyHint>`) | ux | Info | confirmé | nouveau |
| UXS-07 | Onglet « Conflits » placé **avant** l'écran d'accueil du module (route index = Calendrier, `routes.tsx:146` vs `MatchesLayout.tsx:86-106`) — décision fondateur du 15/09, à réévaluer à l'usage | ux | Faible | confirmé | nouveau |
| A11Y-21 | Cibles `h-7` = 28 px (17 sites) : conforme AA 2.5.8, échoue AAA 2.5.5 (roadmap P4-217) | ux | Info | confirmé | nouveau |
| DOC-40 | Graduation non faite : `specs/evolution/console-superadmin.md:32-105` garde ~75 l. d'état LIVRÉ dont la vérité courante est `specs/courantes/superadmin-auth.md` | doc | Faible | confirmé | nouveau |
| DOC-41 | `.claude/rules/frontend.md:8` : compte volatil faux de 69 % (« 315 lignes » vs 532) dans une règle auto-chargée, contre la règle « no volatile counts » | doc | Mineure | confirmé | nouveau |
| DOC-42 | Les lots de dépendances ne laissent aucune ligne dans `etat-des-lieux.md` §3 (PR #900/#901 : contrat OpenAPI 3.1 → 3.2, 9 descriptions publiées) ; l'exemption est réelle dans l'outillage (`skills/dependabot`) mais écrite nulle part | doc | Mineure | confirmé | nouveau |
| DOC-46 | `constraint-vocabulary.md:137` renvoie `FACILITY_CAPACITY` à `main.py:291-294` ; c'est `:488-491` | doc | Faible | confirmé | nouveau |
| DOC-47 | La table d'alignement ignore `targetTags`/`excludeTags` (« et aussi » / « sauf », P2-29) — émis (`ConstraintsStep.tsx:250-254`), résolus (`ScheduleConstraintBuilder.php:1457-1469`), jamais vérifiés par construction | doc/align | Faible | confirmé | nouveau |
| DOC-48 | `constraint-emission.md:90` se contredit : la synthèse dit encore « 🔴 `max_consecutive_days` » alors que sa propre table et le code le donnent fermé (P2-42) | doc | Faible | confirmé | nouveau |
| DOC-43 | `specs/evolution/duplications-de-verite.md` : 49 ✅ / 5 ⬜ — cimetière de résolu en `evolution/` | doc | Info | confirmé | nouveau |
| DOC-44 | `docs/upgrades.md` journal non borné (9 entrées, 34 Ko), sans règle de bornage — même genre que l'ex-DOC-34 | doc | Info | confirmé | nouveau |
| INF-04 | **Aucune politique de redémarrage sur les 12 services de dev** (prod : 10 `unless-stopped`) — constaté en incident : le worker Messenger est resté mort 5 h le 17/09 après un `cache:clear` (sortie code 0 puis code 1) ; PR #918 corrige le worker seul, roadmap P4-220 pour les autres | infra | Faible | **constaté en réel** | nouveau |
| INF-05 | Retour arrière de déploiement non exercé : 148 `down()` jamais joués en CI, 5 migrations irréversibles, 51 `DROP TABLE` en `up` ; le seul chemin de secours est la restauration de la sauvegarde pré-migration (obligatoire, `remote-deploy.sh:68-80`) — nouvel axe candidat, rendu redevable | infra | Faible | confirmé | nouveau |

**2 nouveaux ≥ Élevée (ALIGN-11, BCK-24)** — contre 1 le 08-27 ; **11 Moyennes**, toutes contre-vérifiées à la main ou mesurées.

---

## Tableau de posture cybersécurité (A1–A23)

| # | Attaque | Verdict | Preuve `fichier:ligne` | SEC- |
|---|---|---|---|---|
| A1 | Accès cross-tenant / IDOR | **protégé** | `TenantFilterListener.php:110-134` (forme UUID validée puis appartenance active → 403) ; 3 tables tenant neuves avec `club_id NOT NULL` + ENABLE+FORCE + `tenant_isolation`/`admin_all` dans la migration (`Version20260828120000:44-63`, `…0831120000:34-60`, `…0915120000:37-56`) ; `ConflictResolution` unique `(club, season, fingerprint)` (`:33`) ; empreinte de résolution refusée si absente du flux courant (`FixtureConflictsController.php:159-162`) | — |
| A2 | Brute-force `/login` | **protégé** | inchangé (`security.yaml:31-32` max 5 ; `admin_auth` 5/15 min) | — |
| A3 | Énumération de comptes | **protégé** | 4 limiteurs anti-énumération (`rate_limiter.yaml:5-25`) + tests bloquants | — |
| A4 | Falsification JWT | **protégé** | RS256 (`lexik_jwt_authentication.yaml:2-4`), `git ls-files backend/config/jwt` = 0 ; `lexik:jwt:generate-token` console seule, aucune route ne le porte (utilisé par cet audit pour un jeton de lecture de 20 min) | — |
| A5 | Escalade de privilège | **protégé** | `assertManager()` en tête de **toutes** les écritures neuves (`OpponentRefreshController:73`, `OpponentTravelController:90,128,170,209`, `FixtureConflictsController:135,188`, `ReviewFixtureDeviationController:56`, `VenueExternalLabelController:110,196`) | — |
| A6 | Mass-assignment | **protégé** | `FixtureInput` sans `clubId`/`seasonId`/`opponentOrganismeCode` bindable ; code organisme posé serveur seul (`OpponentLocationResolver.php:257-261`) ; note de résolution ≤ 500 car. + `enum::tryFrom` (`FixtureConflictsController:147-159`) | — |
| A7 | Injection SQL | **protégé** | GUC lié (`TenantConnectionContext.php:28-32`) ; 4 requêtes natives neuves paramétrées (`OpponentVenueSuggestionRepository.php:77-154`) ; empreinte contrainte à la route (`:65` regex) | — |
| A8 | XSS | **protégé** | `dangerouslySetInnerHTML` : 1 hit = commentaire de test ; libellés FBI/FFBB en texte React | — |
| A9 | CSRF | **protégé** | cookie `BEARER` `samesite: strict` + `httpOnly` + `secure` piloté (`lexik_jwt_authentication.yaml:11-16`), `JWT_COOKIE_SECURE=true` en prod | — |
| A10 | DoS bombe de génération | **protégé** | `GenerationComplexityGuard` + caps Pydantic ; `/api/fixtures/place` : payload construit serveur, lock 90 s, timeout 60 s ; réserve : build non borné (ENG-40) et deux listes sans `max_length` (ENG-45) | — |
| A11 | Spam routes anonymes | **protégé** | inventaire `PUBLIC_ACCESS` relu (`security.yaml:43-77`), aucune route publique neuve | — |
| A12 | SSRF | **protégé** | 3 clients sortants, hosts en dur, `max_redirects: 0`, timeouts 5-8 s, inputs range-validés formatés serveur (`FfbbApiClient.php:150-162,223,241`) ; frontend muet | — |
| A13 | Abus d'upload | **partiel** *(était protégé — critère durci sur surface neuve)* | logo inchangé (allowlist + finfo + 500 KB) ; **import xlsx** : mime OU extension, pas de taille/lignes applicatives, feuille entière en mémoire ; XXE réfutée (reader épinglé) ; route management+socle-gated | SEC-22 |
| A14 | Fuite Mercure | **protégé** | dev non-anonyme, CORS localhost, 127.0.0.1, image **épinglée v0.24.2** (`docker-compose.yml:336`) ; prod v0.19 + clés obligatoires + `MercureHardeningTest` | — |
| A15 | Exposition de secrets | **protégé** | `git ls-files backend/config/jwt` = 0 ; `.env*` trackés = templates + `.env.prod.gpg` (chiffré symétrique, source de vérité documentée `docs/ops/prod-stack.md:25`) ; `backend/.env.prod` relu ligne à ligne = 0 valeur secrète | — |
| A16 | Erreurs verboses | **protégé** | `APP_DEBUG=0` ; import : exception loggée, message générique (`ImportFixturesController.php:40,95-99`) ; corruption de donnée source → 422 parlant (`ReviewFixtureDeviationController:91-95`) | — |
| A17 | Clickjacking / en-têtes | **protégé** | CSP stricte (`csp.conf:32`), `X-Frame-Options DENY`, nosniff, Referrer-Policy, HSTS 1 an ; CORS par variable | — |
| A18 | Dépendance vulnérable | **protégé** | **0 vuln aux 3 audits exécutés ce jour** (npm conteneur, composer, pip-audit) ; lot Dependabot #900/#901 traité la veille (Symfony ramené sur 7.4, API Platform 4.4) | — |
| A19 | Usurpation d'approbation de club | **protégé** | inchangé (token 32 octets, limiteur IP, 404 byte-identique) | — |
| A20 | Compromission chaîne de déploiement | **partiel** *(était protégé — critère durci)* | actions épinglées par SHA, `pull_request` (jamais `pull_request_target`), `deploy.yml` avec `permissions:` ; **manque** : `ci.yml`/`security-weekly.yml` sans bloc `permissions:` — contre-vérifié : `default_workflow_permissions = read` → jeton hérité en lecture seule, exposition nulle en l'état | SEC-21 (Mineure) |
| A21 | Empoisonnement communautaire — échéances | **partiel** *(inchangé)* | `upsertShared()` last-write-wins, aucune provenance, pas de limiteur dédié | BCK-18 |
| **A22** | **Empoisonnement de l'annuaire / des salles adverses partagés** (`opponent_directory`, `opponent_venue_suggestion`, hors RLS, lus par tous) — *ligne neuve* | **partiel** | **#904 étanche** : libellé/ville/coords **re-résolus serveur** par `FfbbSalleResolver::resolveByExternalRef` (numéro exact, geoRadius 2 km), AUTO n'écrit jamais le partagé (`OpponentVenueAutoLocator.php:33-38`), `ON CONFLICT` premier writer fédéral gagne, lecture cross-club = ville + compte + date sans heure ; **8 tests** `OpponentVenueSuggestionShareTest`. Résidu : l'**appariement** est choisi par le club, ligne MANUAL permanente sans provenance ; repli `?? $name` dans l'annuaire (BCK-26) | SEC-20, BCK-26 |
| **A23** | **Amplification d'appels sortants / mutation du partagé via « épingler un gymnase »** (`POST /api/opponents/travel/manual`) — *ligne neuve* | **partiel** | `assertManager()` + validation, puis 1 FFBB + 1 IGN + jusqu'à 2 mutations du compteur **sans limiteur dédié** (contraste : 4 routes sœurs bornées `rate_limiter.yaml:53-86`) ; borne effective `api` 300/min | SEC-19 |

**Bilan cyber : 18 protégé · 5 partiel · 0 absent · 0 non vérifié.** Vs 08-27 (20/1/0/0) : 2 lignes neuves nées avec les tables fédérales partagées et la route d'épinglage, 2 verdicts durcis (A13, A20) par critère plus sévère sur surface neuve, **aucune régression de code** sur une ligne existante. La correction #904 (la première MEDIUM de la série, trouvée par la revue interne) est vérifiée étanche aux trois écrivains.

---

## Détail par critère

### 1. Documentation — 82/100 (exactitude 83 · structure 74 · utilité IA 85 · cycle specs 86)
**Forces.** `CLAUDE.md` 6/6 exact sur ses affirmations chargées (versions 4.4/3.7, contrat 2.21 aux 3 sites + garde, priorité 7, zéro proxy, blocking-tests ⇄ ci.yml diffés programmatiquement, racine) ; les 3 docs d'alignement exacts **au numéro de ligne** ; 0 stamp menteur sur 38, stamp le plus vieux à 3 jours ; DOC-34/35/36 corrigés et tracés ; aucune doc vivante morte ; la duplication RLS arbitrée avec sa leçon écrite ; `docs/upgrades.md` est le meilleur document du dépôt pour son public. Cycle specs : initiales figées, compteur roadmap 68 = 68, 5/5 ids récents réellement ouverts au code, 10/11 livraisons tracées.
**Faiblesses.** Le motif de l'édition : **le dépôt corrige le cas et laisse la règle ouverte** — DOC-36 corrigé dans CLAUDE.md, pas dans la carte (DOC-37) ; DOC-34 borné dans un fichier, réincarné ×3 dans `module-matchs.md` (DOC-38) ; le garde de version élargi une fois, pas à la naissance des 3 docs qui ont dérivé (DOC-39). Et **DOC-45** : deux docs de référence annoncent des poids faux depuis 34 jours sous trois stamps « re-confronté » — la rotation vérifie les symboles, pas les valeurs.

### 2. Pertinence du besoin — 92/100
La passe de tests du fondateur des 16-17/09 est le meilleur cycle observé : sept constats → sept PR en trois jours, chacune adossée à un cas réel de la base pilote (Mara le 8/11, U21M2 à JDR, Maxime le 19/09, « trajet inconnu » ×6), décisions produit tranchées et fermées (jamais de réparation silencieuse ; ville, jamais gymnase ; échauffement même gymnase ; défauts du Calendrier). L'argument marketing « conflits détectés + saisie FBI suivie » est né de cette passe et consigné. Réserves constantes : la landing n'est toujours pas publiée (quatrième édition), P5-13 (reproduire les 3 plannings réels) reste le juge de paix, et ALIGN-11 montre que le wizard — le flux le plus ancien — porte une promesse que l'écran n'avoue pas.

### 3a. Code backend — 72/100 (correction+sécu 76 · architecture 62 · tests 78 · robustesse 72)
**Forces.** 12 migrations, 100 % conformes RLS, `down()` partout, rôle résolu dynamiquement ; correctif #904 étanche et falsifié (8 tests) ; routes custom avec `assertManager()` **avant** cap et limiteur, appartenance métier au-delà du tenant (`isAwayOpponent`, empreinte du flux courant), canal temporel fermé (date sans heure) ; verrous compare-and-delete Lua ; méta-gardes de première classe (`TenantOwnedInterfaceCompletenessTest` égalité stricte, `RgpdExportCompletenessTest`, `SymfonyStackAlignmentTest` à 3 niveaux, `RoadmapIdentityTest`, `DocStampFreshnessTest`). Le rail « adversaire multi-gymnases » est, sur l'axe données partagées, le meilleur travail du dépôt.
**Faiblesses.** **BCK-24** : l'effacement RGPD ne tient pas sa promesse écrite, et l'asymétrie export (méta-gardé) / purge (liste manuelle) garantit la récidive. **BCK-23** : le chargeur de radar copié entre deux appelants a déjà divergé, le docblock ment, et le test « Parity » ne compare rien. **BCK-19** : un seul démembrement (bien fait) contre quatre god-services qui grossissent et deux entrants ; 44 → 54 fichiers > 300 l., +14,5 % de masse. Sécurité : SEC-19 (limiteur manquant), BCK-26/27, BCK-28 (six exceptions avalées sans log).

### 3b. Code engine — 85/100 (correction+sécu 90 · architecture 77 · tests 86 · robustesse 83)
**Forces.** La reco P1 livrée au-dessus du cahier : registre de parité AST **à deux niveaux** (familles + arguments), exceptions gardées contre la péremption ; ENG-36/37/38 fermés avec test nommé ; 13/13 clés parsées consommées et **prouvées par effet** côté backend ; trois rails en `to_thread` avec trois sémaphores et leur résidu écrit ; verrous par club avec la subtilité des waiters ; diagnostics INFEASIBLE qui nomment gymnase/coach/équipes et proposent une sortie ; zéro `exec`/`subprocess`, deux `except Exception` tous deux testés, 500 étanche gardé par assertion.
**Faiblesses.** **ENG-41** : l'unique exemption du registre est fausse et pré-couvre exactement l'évolution prévue (ALIGN-10) — un défaut de *garde*, la catégorie que la série punit le plus. **ENG-42** : dernier chemin muet du parseur. **ENG-40** : le rail synchrone n'a pas de borne de construction ni de gate perf (non mesuré). Architecture : ENG-39 déplacé, pas résorbé (`diagnostics.py` 1 614) ; deux géométries d'échauffement (P4-215 documentée, ALIGN-12 non). Aucun test n'entre par HTTP ; bandit hors CI.

### 3c. Code frontend — 84/100 (correction+sécu 86 · architecture 80 · tests 86 · robustesse 82)
**Forces.** Exécuté : tsc/eslint 0/0, **3024 tests / 293 fichiers verts au run 1, 0 retry** (+840 en 22 jours). FRT-27/29 corrigés, UXC-18/19, UXS-05/06, A11Y-17/18/19 corrigés avec l'ID d'audit en commentaire. Filets globaux `MutationCache`/`QueryCache` (l'absence d'`onError` n'est jamais silencieuse), `readState` sur les lectures fondatrices des 3 pages neuves, stream Mercure ré-authentifié et ref-compté. Clavier réel : 62 Tab sans une cible sans anneau ; modale FBI : 12/12 dans le dialog, Échap ferme, focus rendu.
**Faiblesses.** **FRT-30** (setState du parent pendant un rendu, sur l'écran principal), **FRT-31** (gardes de placement front-only sur lectures `?? []`, sans contre-vérification serveur), **FRT-32** (miroir non déclaré de l'enveloppe ligue qui *décide*), FRT-37 (un bandeau abat la route). Cycles inter-features grossis sans garde (wizard→matches ×8), hooks dupliqués, `matches/api.ts` 1 316 l., hook-mocking 63 fichiers, 5/73 tests d'erreur dans matches.

### 4. Supply chain — 96/100
0 vulnérabilité aux trois audits exécutés (npm dans l'image tooling, composer, pip-audit installé à la volée dans le conteneur engine — il n'y est pas préinstallé). Lot Dependabot traité la veille avec le piège Flex habituel (18 paquets Symfony 8.0 ramenés sur 7.4 dans le conteneur) et API Platform 4.4 sorti le jour même (contrat OpenAPI 3.2, 2 descriptions publiques réécrites, P4-218). Retenue inchangée : pas de SBOM, paquet local hors pip-audit.

### 5. Performance solveur — 88/100
Behat complet : **60/60 scénarios, 316 étapes, 433 s de mur** sous sandbox — génération de saison COMPLETED, overlay COMPLETED, reprise COMPLETED, premier planning d'onboarding COMPLETED. Le smoke-solver historique (score 21640) a été remplacé par la feature Gherkin (P4-165) : la série de scores s'arrête, la série de verdicts COMPLETED continue. Retenues : `/place-matches` sans gate perf ni borne de build (ENG-40) sur un rail synchrone, `/api/opponents/refresh` en minutes de mur au pire cas (BCK-32), harnais de charge toujours jamais lancé.

### Cybersécurité — voir tableau A1–A23 : 18 protégé · 5 partiel
La surface a grandi sur quatre fronts (2 tables fédérales partagées, 6 routes custom, 1 import xlsx, 1 pin d'image) sans qu'une ligne existante ne se dégrade en code. La classe de risque ouverte le 08-27 (intégrité communautaire, A21) s'étend à trois tables ; la réponse du dépôt est bonne sur le **contenu** (fédéral, re-résolu serveur, testé) et absente sur la **provenance** et le **débit** (A22/A23). Un seul défaut ≥ Moyenne (SEC-19), contre-vérifié.

### RGPD — couvert, **1 finding Élevée**
Mécanismes inchangés (9 purges, export dérivé du marqueur, effacement à +30 j, audit trail 12 mois). **BCK-24** : `opponent_travel` (code organisme adverse, gymnase choisi, coordonnées, minutes de trajet depuis le siège du club) survit à l'effacement du club et au reset de saison. Périmètre PII inchangé par ailleurs (les coordonnées de gymnases et les villes d'adversaires sont des données d'établissement/fédérales). Roadmap P4-208 le connaît depuis le 17/09 ; l'audit le grade Élevée parce que la promesse écrite est fausse et qu'aucun garde n'empêche la prochaine entité de suivre le même chemin.

### UX — cohérence 80 · simplicité 75 · inclusivité 72 → général 72
**Cohérence (80)** : le module matchs a fait converger son vocabulaire (un seul mot « Conflits », dates françaises partout, un seul « écart », un seul « coach », 0 jargon technique visible — l'axe le plus sain du dépôt, `HARD`/`socle`/`slot` n'existent plus qu'en commentaire ou en identifiant). Résidus Faibles : la puce de filtre recodée 6 fois (UXC-20), `FilterToggle` à 1 consommateur (UXC-21), « salle » pour le gymnase du club (UXC-23), trois mots pour `UNPLACED` selon l'onglet (UXC-24). La console superadmin est rentrée dans le design system avec un garde.
**Simplicité (75)** : les 4 flux-clés sont courts et guidés (placer un match en 2-3 clics avec un bouton qui change de nom selon le geste réel ; import → file d'arbitrage en 8 clics avec raccourcis). Plafonné par **UXS-08** : l'échec de chargement rendu comme « rien à configurer » sur Configuration et Semaine type — le motif corrigé sur le Calendrier (UXS-05) s'est déplacé, avec un conseil faux et actionnable à la clé (« ajoutez un accès »).
**Inclusivité (72)** : le parcours réel est bon là où il compte (focus visible sur 62 cibles, ordre logique, piège de focus correct, `role="group"` tous nommés, 37/37 boutons icône labellisés, icônes lucide `aria-hidden` par défaut). Plafonné par **A11Y-22, mesuré** : 2,56 sur les cases d'habitude, 4,13 sur le badge « à traiter » en clair, 4,33 sur la grille de planning sombre — du texte informatif, pas décoratif. Plus deux défauts axe sur « Semaine type » (A11Y-23/24). Pas de lecteur d'écran.

---

## Avis global + axes priorisés

**L'édition de la surface qui déborde ses gardes.** Le registre a fonctionné mieux que jamais (19 fermés, dont l'Élevée précédente fermée *par sa règle*), le produit a gagné trois mois de valeur en trois semaines, et la note baisse de 5 points. Ce n'est pas contradictoire : la barre est le commercialisable, et deux choses ne le sont pas aujourd'hui. **Un gestionnaire peut bannir un gymnase pour tout son club en croyant exprimer une préférence** (ALIGN-11), et **effacer un club ne l'efface pas** (BCK-24). Ni l'une ni l'autre n'est un bug de code : ce sont des listes tenues à la main (les exemptions du wizard, les entités de la purge) dans un dépôt qui a démontré, cette édition même, qu'il sait remplacer une liste par un garde (`test_hard_layer_parity_registry`, `RgpdExportCompletenessTest`, `consolePalette.guard.test`). Les trois recos P1 ci-dessous sont trois gardes, pas trois correctifs.

| Reco | Priorité | Effort | Traité |
|---|---|---|---|
| ENG-36 — `venue_travel_times` dans `_apply_hard` + test verdict×travel | ~~P1~~ | — | ✅ (`validate_assignments.py:485`, `test_validate_travel_time.py`) |
| Registre de parité du verdict (diff familles HARD, exceptions nominatives) | ~~P1~~ | — | ✅ **au-dessus du cahier** (familles + arguments, `test_hard_layer_parity_registry.py`) |
| FRT-27 — `onError` sur les 3 hooks de la matrice | ~~P1~~ | — | ✅ (+ filet global `MutationCache` déjà présent) |
| Geler `CustomRoutesOpenApiFactory` (découpe par domaine) | ~~P1~~ | — | ✅ (2 846 → 90 l., 19 contributeurs) — mais les 4 autres god-services ⬜ (BCK-19) |
| UXS-05 — readState/LoadErrorHint sur MatchesPage | ~~P2~~ | — | ✅ sur le Calendrier ; ⬜ déplacé sur Configuration/Semaine type (UXS-08) |
| UXC-18 — un seul mot pour litige/conflit | ~~P2~~ | — | ✅ « Conflits » |
| UXC-19 — frDate sur PlacementPanel/UnplacedList | ~~P2~~ | — | ✅ |
| DOC-34 — borner le meta-changelog OpenAPI | ~~P2~~ | — | ✅ (8 entrées, règle dans le fichier) |
| Hook off-site backups | ~~P2~~ | — | ✅ `BACKUP_SYNC_COMMAND` dans `DatabaseBackupCommand` + runbook §4bis (pas de VM pour l'exercer) |
| ENG-37 — diagnostic 4ᵉ consommateur de la source travel | ~~P3~~ | — | ✅ |
| FRT-22 — dédupliquer les types API | P2 | M | ⬜ reconduit (3 copies ; `level` corrigé) |
| BCK-18 — provenance minimale sur l'échéance partagée | P2 | S | ⬜ reconduit — s'étend désormais à 3 tables (SEC-20) |
| BCK-22 — timeout global d'autofill | ~~P3~~ | — | 🟡 partiel (budget 30 s posé) ; le pire cas renaît sur `/opponents/refresh` (BCK-32) |
| ALIGN-10 — trancher MIN_SESSIONS plancher dur ou abandon documenté | P3 (produit) | M | ⬜ reconduit — **et lire ENG-41 avant** : l'exemption du registre le couvrirait en silence |
| Fond de sac 08-27 : DOC-35/36 ✅ · BCK-20 ⬜ · BCK-21 🟡 · ENG-39 🟡 · FRT-28 🟡 · FRT-29 ✅ · UXC-10 🟡 · UXC-12 ✅ · UXS-06 ✅ · A11Y-16 ⬜ · A11Y-17/18/19 ✅ · FRT-20 ⬜ | P3 | S | — |
| **ALIGN-11 — dire l'exclusivité à l'écran** : quand « préfère » + Obligatoire (ou « impose ») cible un groupe, le wizard nomme la règle « impose Gymnase Y **et le réserve** au groupe X » et affiche le nombre d'équipes exclues avant validation ; OU renoncer à l'exclusivité automatique et en faire un mode explicite | **P1** | S | ⬜ nouveau |
| **BCK-24 — `SeasonDataPurgerCompletenessTest` + `ErasedClubPurgerCompletenessTest`** calqués sur `RgpdExportCompletenessTest` (toute entité `TenantOwnedInterface` est purgée ou exclue nommément avec sa raison), puis ajouter `OpponentTravel` aux deux purgers | **P1** | S | ⬜ nouveau (roadmap P4-208 pour la purge seule) |
| **ENG-41 — retirer l'exemption `min_sessions_by_team` du registre** (les deux côtés passent des zéros) pour que le jour d'ALIGN-10 fasse rougir le garde ; à défaut réécrire sa raison pour qu'elle dise le vrai | **P1** | S | ⬜ nouveau |
| **BCK-23 — `ConflictRadarLoader` unique** consommé par `FixtureConflictsController` et `MatchModuleDeltaComputer`, et `MatchVisitDeltaParityTest` qui compare enfin les deux ensembles d'empreintes | **P1** | M | ⬜ nouveau (roadmap P4-211) |
| **A11Y-22 — remplacer `text-*/NN` par des tokens AA** (`text-muted-foreground` plein, ou un token `--muted-foreground-soft` mesuré ≥ 4,5:1) sur les 11 sites + le badge `bg-warning/15` ; gater les paires `/5`, `/15` et `opacity-60` dans `a11y-contrast.spec.ts` | **P1** | S | ⬜ nouveau (P4-213 réfuté sur `/5`, vrai sur `opacity-60`) |
| ENG-42 — famille inconnue → `parse_warning` comme les 6 autres branches | P2 | S | ⬜ nouveau |
| FRT-31 — `readState` sur les lectures qui GATENT un geste (fenêtres d'accès, indisponibilités, enveloppe) + refus serveur dans `FixtureStateProcessor` (fenêtre d'accès, indisponibilité) — le radar ne doit pas être la seule garde | P2 | M | ⬜ nouveau |
| FRT-32 — déclarer le miroir enveloppe dans `FrontRederivationRegistryTest` avec un test de parité (patron `matchAccess.parity.json`), ou consommer `LEAGUE_WINDOW_VIOLATION` servi | P2 | S | ⬜ nouveau |
| FRT-30 — déplacer `onHighlight` hors du rendu (effet dédié, ou remonter l'état) | P2 | S | ⬜ nouveau |
| UXS-08 — `readState`/`LoadErrorHint` sur Configuration et Semaine type (patron `CalendarPage.tsx:430-446`) | P2 | S | ⬜ nouveau |
| SEC-19 — limiteur dédié sur `/opponents/travel/manual` (aligné sur `/resolve`) | P2 | S | ⬜ nouveau |
| DOC-45 — corriger +10/−10 dans les 2 docs, et **ajouter les valeurs de poids au garde** `test_contract_version_doc_sync` (ou un test qui lit `weights.py` et grep les docs) | P2 | S | ⬜ nouveau |
| DOC-38 — refondre `module-matchs.md` en état courant par écran (le journal daté vit dans `etat-des-lieux.md` §3), avec une règle de taille | P2 | M | ⬜ nouveau |
| DOC-37/39 — carte engine à 6 routes ; élargir `DOCS_QUOTING_THE_ACTIVE_VERSION` aux 4 docs hors filet (ou grep automatique de `2\.\d+` sur `docs/**`) | P2 | S | ⬜ nouveau |
| ENG-40 — borner le build de `/place-matches` (horizon daté côté builder, garde sur candidats) + un cas perf sur une saison complète | P2 | M | ⬜ nouveau |
| ALIGN-12 — envoyer le trajet adversaire à `/place-matches` (ou documenter la divergence comme P4-215) | P2 (produit) | M | ⬜ nouveau |
| INF-05 — drill « migrate → down → up » sur base temporaire, une fois par édition | P3 | S | ⬜ nouveau (axe candidat) |
| Fond de sac : ENG-43/44/45/46/47 · BCK-26/27/28/29/30/31/32 · SEC-20/21/22 · FRT-33/34/35/36/37 · UXC-20/21/22/23/24 · UXS-07 · A11Y-21/23/24 · ALIGN-13/14/15 · DOC-40/41/42/43/44/46/47/48 · INF-04 (PR #918 en cours) | P3 | S | ⬜ |

## Features intéressantes à développer (valeur/effort)

1. **Publier la landing** (P5-5) — quatrième édition d'affilée où il ne manque que le geste d'ops ; l'argument matchs consigné le 17/09 (« ton coach joue à la même heure que son équipe ? tu le sais en septembre ») est prêt à servir.
2. **Le trajet adversaire dans le placement automatique** (ALIGN-12) — le radar le sait, la matrice existe, le solveur de placement est le seul à l'ignorer ; c'est la dernière asymétrie entre « ce que l'appli signale » et « ce qu'elle propose ».
3. **Un `FilterChip` partagé** (UXC-20, P4-216) — 6 copies dans 2 fichiers, et la mémoire de session sur l'onglet Conflits (P4-219) va en ajouter ; le geste qui ferme les deux.
4. **Lancer le harnais de charge** (`backend/scripts/load-test/`) — toujours jamais couplé à l'audit ; l'axe coûts reste vide par manque de la seule donnée qu'on peut produire sans prod.
5. **Un drill de retour arrière** (INF-05) — 148 `down()` jamais joués ; une base temporaire, `migrate` → `down` → `up`, parité de schéma : le pendant du restore drill du 08-27.

---

## Annexe méthodologie

**Exécuté** : `tsc -b --force` + eslint (0/0) et `vitest run` (3024 tests / 293 fichiers, 100 % verts, 0 retry, image tooling rebâtie avant — via l'agent frontend) ; `npm audit --omit=dev` (conteneur tooling) + `composer audit` + `pip-audit` (conteneur engine, installé root à la volée) : 0 vuln ×3 ; **Behat complet** sous `with-sandbox.sh` (60/60, 316 étapes, 433 s, mode play restauré) ; **parcours navigateur** sur la base réelle du club pilote (chromium du projet + `@axe-core/playwright`, compte gestionnaire par jeton console de 20 min, `POST /api/matches/module-visit` intercepté avec une réponse de forme valide pour ne pas horodater la visite — la première passe l'avait interceptée avec `{}` et a révélé FRT-37 ; 11 écrans × 2 thèmes ; 2 trails clavier de 30 Tab ; piège de focus de la modale FBI ; 0 écriture serveur, jeton supprimé) ; `gh api …/actions/permissions/workflow` (SEC-21) ; `docker inspect`/`ps` (INF-04). **Statique** : backend, engine, doc, alignement, cyber (agents lecture seule).

**Contre-vérifiés à la main (Étape 3)** : ALIGN-11 (les 3 couches relues : `ConstraintsStep.tsx:376-386`, `ScheduleConstraintBuilder.php:1360-1392`, `parsing.py:313-333`) ; BCK-24 (re-grep `OpponentTravel` = 0 dans les deux purgers, 45 entités tenant) ; BCK-23 (les deux appels `detect()` lus côte à côte) ; ENG-41 (`main.py:516-532` : zéros des deux côtés) ; ENG-42 (`parsing.py:449-463` : `logger.warning` seul) ; SEC-19 (méthode `manual()` relue en entier, aucun limiteur, `resolve()` en a un) ; SEC-21 (`default_workflow_permissions = read` → rétrogradé) ; A22 (9 consommateurs des tables partagées listés) ; A11Y-22 (mesuré par axe en 2 thèmes) ; A11Y-23/24 (axe) ; FRT-37 (reproduit) ; UXS-08 (`grep readState` = 0 sur les deux pages) ; DOC-45 (`weights.py:53,61` lus).

**Limites** : pas de lecteur d'écran (clavier réel + axe seulement) ; ENG-40 gradé Moyenne **sans mesure** (pas de chrono sur une saison réelle) ; pas de phpunit/pytest lancés par l'audit lui-même hors Behat (la CI de main était verte ce jour, 2314 tests backend verts la veille sur le lot Dependabot) ; le restore drill n'a pas été rejoué ; la première passe navigateur a été jouée pendant que Behat avait basculé la stack en bac à sable — invalidée et rejouée après restauration du mode play ; A20 vérifié sur le code et l'API des permissions, pas sur une exécution ; le harnais de charge toujours non lancé.

**Confiance par axe** : élevée = frontend (exécuté), supply chain (exécuté ×3), perf (Behat exécuté), inclusivité (axe + clavier exécutés), UX-simplicité (parcours réel + proxys), cyber (preuves recoupées + 3 contre-vérifications dont une via l'API GitHub) · moyenne = backend, engine, doc, alignement, UX-cohérence (lecture de code, recoupée entre agents et contre-vérifiée sur toutes les Moyennes/Élevées) · faible = ENG-40 (lecture seule, non mesuré), coûts (non couvert).

**Auto-question de biais (honnête)** : (1) Les deux Élevées sont des lectures de code, pas des reproductions : je n'ai pas généré un planning avec « préfère + Obligatoire » sur un groupe pour voir 20 équipes bannies, ni effacé un club pour compter les lignes `opponent_travel` survivantes — les trois couches et les deux purgers lus suffisent à la logique, pas à l'ampleur ressentie. (2) La baisse de note vient surtout du backend, noté par un agent sévère sur l'architecture (62) ; un lecteur pourrait juger que +12 000 lignes pour sept livraisons validées par le fondateur méritent plus — j'ai gardé la sévérité parce que la duplication a déjà divergé (BCK-23), ce qui n'est pas une question de goût. (3) Sur-poids du greppable, toujours : « 0 `subprocess` », « 0 `dangerouslySetInnerHTML` » sont des greps ; les verdicts cyber restent des lectures, jamais des attaques simulées. (4) Le parcours navigateur a été joué avec un jeton minté en console et un POST intercepté : c'est le seul moyen de ne rien écrire chez le fondateur, mais ce n'est pas *exactement* le chemin d'un utilisateur réel (le bandeau « depuis votre dernière visite » a vu une réponse simulée). (5) Données manquantes : coûts réels, comportement multi-clubs simultanés, temps de build réel de `/place-matches` sur une saison complète, et un lecteur d'écran.
